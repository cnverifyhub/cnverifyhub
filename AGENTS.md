# CNWePro Agent Guide (v2.0 - Brain Layer Active)

## Brain Layer
- **Resilience**: `resilience-guardian` + `crash-recovery` (heartbeat).
- **Memory**: `agent-memory` + `task_graph.json` (persistence).
- **Reasoning**: `hermes-cognition` (self-improvement).
- **Style**: Ultra-dense "Caveman" syntax. Token efficiency priority.

## Project Core
- **Stack**: Next.js 14, TS, Supabase. Tailwind v3 only.
- **Deploy**: GitHub push ONLY. NO auto-Vercel build.

## Commands
| Cmd | Result |
|---|---|
| `npm run dev` | Localhost:3000 |
| `npm run sync` | node scripts/sync-products.js |

## Critical Patterns
- **Sync**: Admin Dashboard > Inventory > "Sync from Code".
- **Edit**: Admin Dashboard > Inventory > "Full Edit" (Name/Price/Stock).
- **CTAs**: Hero section dual Telegram buttons (Channel/Personal).
- **Fraud**: Blocklist (email/IP/TXID) + velocity checks.

## Skills (Global)
- **Primary**: `superpowers`, `graphify`, `obsidian-skills`.
- **Logic**: `resilience-guardian`, `hermes-cognition`, `crash-recovery`.
- **UI**: `chinese_ui`, `gsap-animations`, `vision-ui-architect`.
